# M5：Vercel Folium 台灣氣溫地圖

沿用 data/data.db，不變更 schema、API parser 或寫入流程。
src/locations.py 管理 22 縣市代表座標，不將座標存入 SQLite。
src/weather_map.py 計算平均溫度並產生 Folium 地圖。
build_site.py 以唯讀連線讀取 SQLite，正規化日期為 YYYY-MM-DD，
產生 public/maps.json、每天的 maps/YYYY-MM-DD.html 與 weather.json。
網頁日期選擇器使用同格式字串切換地圖，日期上下限來自資料庫。
缺少日期顯示提示；缺少座標或無效溫度跳過標記並顯示警告。

## 更新與本機啟動

```powershell
.\.venv\Scripts\python.exe fetch_data.py
.\.venv\Scripts\python.exe build_site.py
.\.venv\Scripts\python.exe -m http.server 8501 --directory public
```

開啟 http://localhost:8501。只重建介面時不需重新抓取 API。
Folium 已列於 requirements.txt，不使用 Streamlit 或 streamlit-folium。

## Vercel

Framework: Other；Build Command: 空白；Output Directory: public。
先執行 build_site.py，再部署包含 public/ 的專案。public/ 為可提交的靜態產物。
此版本為資料快照，Vercel 不會自動讀取或更新 SQLite。
Folium 的 JS/CSS 與 OpenStreetMap 圖磚需要網路。

## 驗收

日期切換控制全臺地圖；地區選單控制原有趨勢圖及資料表。
點擊標記顯示縣市、最低／最高／平均溫度。
色階：<20 藍、20≤T<25 綠、25≤T≤30 橘、>30 紅。
地圖初始視野包含離島。
